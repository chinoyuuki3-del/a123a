{
  "name": "troll-engine-aft-sync-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute aft-sync-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute aft-sync-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
